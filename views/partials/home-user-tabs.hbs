<h1>{{tweets.id}}</h1>
<section id="Huei-region" <div class="nav-user">
  <h4 class="nav-user-title">推薦跟隨</h4>
  <ul>
    <p class="list-separate"></p>
    {{#each users}}
    {{#ifCondFalse this.id ../user.id}}
    <li>
      <div class="d-flex align-items-center user-list mt-4">
        <a href="/users/{{this.id}}/tweets">
          <div class="d-flex">
            <img src="{{#if this.avatar}}{{this.avatar}}{{else}}/images/icon/logo-gray.png{{/if}}" alt=""
              class="circle">
            <div class="d-flex flex-column user-name-position">
              <p class="text-dark position-left-text">{{this.name}}</p>
              <p class="text-black-50 position-left-text">@{{this.account}}</p>
            </div>
            {{#if this.isFollowed}}
            <form action="/followships/{{this.id}}?_method=DELETE" method="POST" class="">
              <div class="d-flex change-icon">
                <button type="submit" class="btn btn-white rounded-pill mt-1 user-follow-bottom icon-change">
                  <span class="">取消追隨</span>
                </button>
                <button type="submit" class="btn btn-orange rounded-pill mt-1 user-follow-bottom icon-unChange">
                  <span class="">正在追隨</span>
                </button>
              </div>
            </form>
            {{else}}
            <form action="/followships" method="POST">
              <div class="d-flex change-icon">
                <button type="submit" class="btn btn-orange rounded-pill mt-1 user-follow-bottom icon-change" name="id"
                  onclick="notifyForAllSubscribers()" value={{this.id}}>
                  <span class="p">追隨</span>
                </button>
                <button type="submit" class="btn btn-white rounded-pill mt-1 user-follow-bottom icon-unChange" name="id"
                  onclick="notifyForAllSubscribers()" value={{this.id}}>
                  <span class="p">追隨</span>
                </button>
              </div>
            </form>
            {{/if}}
          </div>
        </a>
      </div>
    </li>
    {{/ifCondFalse}}
    {{/each}}

  </ul>
  </div>
</section>